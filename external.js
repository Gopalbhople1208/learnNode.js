//how to install the external package command(npm i colors) colors name package is install 
//after is install the package.json ,package-lock.json and node_modules is file is add
//self add the package.json(node init)
const colors = require("colors");

console.log(colors.green("this is call the external package"));
console.log(colors.yellow("this is call the external package"));
console.log(colors.red("this is call the external package"));
console.log(colors.blue("this is call the external package"));



